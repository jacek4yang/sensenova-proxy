[package]
name = "sensenova-proxy"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
authors = ["sensenova-proxy contributors"]
description = "A small robust SenseNova Token Plan gateway for Claude Code (Anthropic Messages API)"
license = "MIT"

[dependencies]
anyhow = "1"
async-stream = "0.3"
axum = { version = "0.8", features = ["macros"] }
bytes = "1"
clap = { version = "4", features = ["derive"] }
futures-util = "0.3"
httpdate = "1"
reqwest = { version = "0.12", default-features = false, features = ["gzip", "http2", "json", "rustls-tls", "stream"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
tokio = { version = "1", features = ["full"] }
tower = { version = "0.5", features = ["util"] }
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "json"] }
uuid = { version = "1", features = ["v4"] }

[dev-dependencies]
proptest = "1"

[profile.release]
lto = "thin"
opt-level = 3
strip = true
